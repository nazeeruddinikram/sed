Fun sed scripts.

cat, fold, head, rev, tac, tail, uniq
    are implementations of the unix commands (no opts)

longest
    outputs longest line in the input
    
palindromes
    outputs all palindromes in input (lines that are the same forward and backwords)
    
parentheses
    checks if the parentheses () match over the input
