#!/bin/sed -f

:a
/^[^()]*(/{
    x
    s/^\(.*\)$/\1(/
    x
    s/^[^()]*(//
}
/^[^()]*)/{
    x
    /^$/ {
        bend
    }
    
    s/($//
    x
    s/^[^()]*)//
}
/^[^()]*$/ !ba
$ !d

${
    :end
    x
    /^[^()]*$/ s/^.*$/Balanced/
    /[()]/ s/^.*$/Unbalanced/
    q
}

